export const metadata = {
  title: 'About', // Overrides the title from `app/layout.tsx`
  // `description` is automatically pulled from `app/layout.tsx`
};

export default function About() {
  return <div>About</div>;
}
